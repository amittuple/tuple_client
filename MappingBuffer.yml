column_map:
  TRANSACTION_MASTER:
    cust_id: from_id
    product_id: to_id
    revenue: actiontype
    timestamp: timestamp
is_factor:
  TRANSACTION_MASTER:
  - actiontype
table_map:
  TRANSACTION_MASTER: event
